import React from 'react';
import Demo from '../demo/demo';

const TemplateBlock = ({ ...props }) => {
  return (
    <div>
      <Demo />
    </div>
  );
};

export default TemplateBlock;

//graphql fragment here however it works in next
